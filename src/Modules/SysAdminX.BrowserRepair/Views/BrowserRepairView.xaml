<Page
    x:Class="SysAdminX.BrowserRepair.Views.BrowserRepairView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:SysAdminX.BrowserRepair.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=viewmodels:BrowserRepairViewModel, IsDesignTimeCreatable=False}"
    Title="Browser Repair">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Browser Repair" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Grid.Row="1" Text="Repair corrupted browsers, clear cache, or reset to default settings." Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,24" />

        <Grid Grid.Row="2">
            <ui:ProgressRing IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Browsers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card Margin="0,0,16,16" Width="380" Padding="16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0" Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <ui:SymbolIcon Symbol="{Binding Icon}" FontSize="32" Margin="0,0,12,0" />
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="18" />
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Status: " FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                                <TextBlock Text="Installed" FontSize="12" Foreground="{DynamicResource SystemFillColorSuccessBrush}" Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                <TextBlock Text="Not Installed" FontSize="12" Foreground="{DynamicResource SystemFillColorCriticalBrush}" Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverted}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <StackPanel Grid.Row="1" Margin="0,0,0,16">
                                        <Grid Margin="0,0,0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Cache Size:" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,8,0" />
                                            <TextBlock Grid.Column="1" Text="{Binding CacheSizeString}" FontWeight="SemiBold" />
                                        </Grid>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Path:" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,8,0" />
                                            <TextBlock Grid.Column="1" Text="{Binding InstallPath}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding InstallPath}" />
                                        </Grid>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" IsEnabled="{Binding IsInstalled}">
                                        <ui:Button Content="Clear Cache" Command="{Binding ClearCacheCommand}" Margin="0,0,8,0" />
                                        <ui:Button Content="Reset" Command="{Binding ResetCommand}" Margin="0,0,8,0" Appearance="Danger" />
                                        <ui:Button Content="Re-register" Command="{Binding ReRegisterCommand}" />
                                        <ui:ProgressRing Margin="8,0,0,0" IsIndeterminate="True" Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </StackPanel>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
