<Page
    x:Class="SysAdminX.SystemRestore.Views.SystemRestoreView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:SysAdminX.SystemRestore.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=viewmodels:SystemRestoreViewModel, IsDesignTimeCreatable=False}"
    Title="System Restore">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:Card Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="System Protection Status" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding IsProtectionEnabled, StringFormat='Protection Enabled: {0}'}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <ui:Button Grid.Column="1" 
                               Icon="Check24" 
                               Content="Enable Protection" 
                               Command="{Binding EnableProtectionCommand}"
                               Visibility="{Binding IsProtectionEnabled, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverted}" />
                </Grid>
                
                <Separator Margin="0,16" />
                
                <TextBlock Text="Create New Restore Point" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0" 
                                PlaceholderText="Description for new restore point..." 
                                Text="{Binding NewPointDescription, UpdateSourceTrigger=PropertyChanged}" 
                                Margin="0,0,8,0" />
                    <ui:Button Grid.Column="1" 
                               Icon="Add24" 
                               Content="Create" 
                               Appearance="Primary"
                               Command="{Binding CreatePointCommand}" />
                </Grid>
            </StackPanel>
        </ui:Card>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Existing Restore Points" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Icon="ArrowClockwise24" Content="Refresh" Command="{Binding LoadDataCommand}" Margin="0,0,8,0"/>
                <ui:Button Icon="History24" Content="Open System Restore" Command="{Binding RestorePointCommand}" />
            </StackPanel>
        </Grid>

        <ui:DataGrid Grid.Row="2" 
                     ItemsSource="{Binding RestorePoints}" 
                     AutoGenerateColumns="False" 
                     IsReadOnly="True"
                     HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sequence" Binding="{Binding SequenceNumber}" Width="Auto"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Date Created" Binding="{Binding CreationTime, StringFormat='{}{0:g}'}" Width="200"/>
                <DataGridTextColumn Header="Type" Binding="{Binding EventType}" Width="150"/>
            </DataGrid.Columns>
        </ui:DataGrid>
        
        <ui:ProgressRing Grid.Row="2" IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</Page>
