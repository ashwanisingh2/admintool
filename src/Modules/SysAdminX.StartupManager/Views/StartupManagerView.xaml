<Page
    x:Class="SysAdminX.StartupManager.Views.StartupManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:SysAdminX.StartupManager.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=viewmodels:StartupManagerViewModel, IsDesignTimeCreatable=False}"
    Title="Startup Manager">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" 
                        Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="Search apps..." 
                        Margin="0,0,8,0">
                <ui:TextBox.Icon>
                    <ui:SymbolIcon Symbol="Search24" />
                </ui:TextBox.Icon>
            </ui:TextBox>

            <ui:Button Grid.Column="1" 
                       Command="{Binding OpenStartupFolderCommand}" 
                       Icon="{ui:SymbolIcon Folder24}" 
                       Content="Open Startup Folder" 
                       Appearance="Secondary"
                       Margin="0,0,8,0"/>

            <ui:Button Grid.Column="2" 
                       Command="{Binding LoadStartupAppsCommand}" 
                       Icon="{ui:SymbolIcon ArrowClockwise24}" 
                       Content="Refresh" 
                       Appearance="Primary"/>
        </Grid>

        <ui:DataGrid Grid.Row="1" 
                     ItemsSource="{Binding StartupAppsView}"
                     AutoGenerateColumns="False" 
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     IsReadOnly="True"
                     HeadersVisibility="Column"
                     GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Status">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:ToggleSwitch IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}" 
                                             Margin="4,0" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="1.5*"/>
                <DataGridTemplateColumn Header="Impact" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="6,2" CornerRadius="4" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Impact}" Value="High">
                                                <Setter Property="Background" Value="#33FF4444" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Impact}" Value="Medium">
                                                <Setter Property="Background" Value="#33FFBB33" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Impact}" Value="Low">
                                                <Setter Property="Background" Value="#3300C851" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Impact}" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Impact}" Value="High">
                                                    <Setter Property="Foreground" Value="#FF4444" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Impact}" Value="Medium">
                                                    <Setter Property="Foreground" Value="#FFBB33" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Impact}" Value="Low">
                                                    <Setter Property="Foreground" Value="#00C851" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Command" Binding="{Binding Command}" Width="3*"/>
            </DataGrid.Columns>
        </ui:DataGrid>

        <ui:ProgressRing Grid.Row="1" IsIndeterminate="True" 
                         Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                         Width="60" Height="60"/>
    </Grid>
</Page>
