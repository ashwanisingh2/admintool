<Page
    x:Class="SysAdminX.SoftwareManager.Views.SoftwareManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:SysAdminX.SoftwareManager.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800"
    Loaded="OnPageLoaded"
    d:DataContext="{d:DesignInstance Type=viewmodels:SoftwareManagerViewModel, IsDesignTimeCreatable=False}"
    Title="Software Manager">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Software Manager" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16" />

        <ui:InfoBar
            Grid.Row="1"
            IsOpen="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"
            Title="Error"
            Message="{Binding ErrorMessage}"
            Severity="Error"
            Margin="0,0,0,16" />

        <TabControl Grid.Row="2" Style="{DynamicResource {x:Type TabControl}}">
            <TabItem Header="Installed Software">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
            
                    <Grid Grid.Row="0" Margin="0,16,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <ui:TextBox
                Grid.Column="0"
                Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                PlaceholderText="Search by name or publisher..."
                Icon="{ui:SymbolIcon Search24}"
                Width="300"
                HorizontalAlignment="Left" />
                
            <ui:Button Grid.Column="1" Command="{Binding LoadSoftwareCommand}" Content="Refresh" Icon="{ui:SymbolIcon ArrowClockwise24}" />
        </Grid>

                    <ui:DataGrid
                        Grid.Row="1"
            ItemsSource="{Binding FilteredSoftwareList}"
            SelectedItem="{Binding SelectedSoftware}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            SelectionMode="Single"
            CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Application Name" Binding="{Binding DisplayName}" Width="300" />
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="200" />
                <DataGridTextColumn Header="Version" Binding="{Binding DisplayVersion}" Width="120" />
                <DataGridTextColumn Header="Install Date" Binding="{Binding InstallDate}" Width="120" />
            </DataGrid.Columns>
        </ui:DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Right" IsEnabled="{Binding SelectedSoftware, Converter={StaticResource NullToBoolConverter}}">
                        <ui:Button Command="{Binding UninstallCommand}" Content="Uninstall" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" />
                    </StackPanel>
                </Grid>
            </TabItem>
            
            <TabItem Header="App Store / Quick Install">
                <Grid Margin="0,16,0,0">
                    <ItemsControl ItemsSource="{Binding PopularApps}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Card Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <ui:SymbolIcon Symbol="{Binding Icon}" FontSize="24" Grid.Column="0" Margin="0,0,16,0" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Name}" Grid.Column="1" FontSize="16" VerticalAlignment="Center" />
                                        <ui:Button Grid.Column="2" Content="Install" Command="{Binding DataContext.InstallAppCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Icon="{ui:SymbolIcon ArrowDownload24}" Appearance="Primary" />
                                    </Grid>
                                </ui:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="0" Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <ui:ProgressRing IsIndeterminate="True" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>
