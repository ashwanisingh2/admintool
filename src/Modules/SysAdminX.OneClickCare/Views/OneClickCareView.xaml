<Page
    x:Class="SysAdminX.OneClickCare.Views.OneClickCareView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:SysAdminX.OneClickCare.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=viewmodels:OneClickCareViewModel, IsDesignTimeCreatable=False}"
    Title="One Click Care">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="One-Click System Care" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Grid.Row="1" Text="Run a comprehensive suite of optimizations and repairs with a single click." Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,24" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,24,0">
                <ui:Button Icon="Play24" Content="Start One-Click Care" Appearance="Primary" Command="{Binding StartCareCommand}" HorizontalAlignment="Stretch" Margin="0,0,0,8" />
                <ui:Button Icon="Stop24" Content="Cancel" Command="{Binding CancelCareCommand}" HorizontalAlignment="Stretch" />
            </StackPanel>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Steps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="16" Margin="0,0,0,8" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ui:SymbolIcon Symbol="{Binding StatusIcon}" FontSize="24" Margin="0,0,16,0" Grid.Column="0" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" />
                                        <ProgressBar Value="{Binding Progress}" Maximum="100" Margin="0,8,0,4" />
                                        <TextBlock Text="{Binding Output}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding EstimatedTimeRemaining}" Grid.Column="2" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
