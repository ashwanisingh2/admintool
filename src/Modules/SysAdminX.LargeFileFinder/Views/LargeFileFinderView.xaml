<Page
    x:Class="SysAdminX.LargeFileFinder.Views.LargeFileFinderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:SysAdminX.LargeFileFinder.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=viewmodels:LargeFileFinderViewModel, IsDesignTimeCreatable=False}"
    Title="Large File Finder">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Large File Finder" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" ItemsSource="{Binding Drives}" SelectedItem="{Binding SelectedDrive}" Width="100" Margin="0,0,16,0"/>
            
            <TextBlock Grid.Column="1" Text="Min Size (MB):" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ui:NumberBox Grid.Column="2" Value="{Binding MinSizeMB}" Minimum="10" Maximum="10000" Width="120" Margin="0,0,16,0" />

            <ui:Button Grid.Column="4" Content="Scan" Icon="{ui:SymbolIcon Search24}" Command="{Binding ScanCommand}" Appearance="Primary" />
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Files}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="File Path" Binding="{Binding FilePath}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Extension}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Modified" Binding="{Binding LastModified, StringFormat=d}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <ui:ProgressRing IsIndeterminate="True" Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}" Margin="0,0,8,0" Width="16" Height="16"/>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
            </StackPanel>

            <ui:Button Grid.Column="1" Content="Move to Backup" Icon="{ui:SymbolIcon FolderMove24}" Command="{Binding MoveSelectedCommand}" Margin="0,0,8,0"/>
            <ui:Button Grid.Column="2" Content="Delete Selected" Icon="{ui:SymbolIcon Delete24}" Command="{Binding DeleteSelectedCommand}" Appearance="Danger"/>
        </Grid>
    </Grid>
</Page>
