<Page
    x:Class="SysAdminX.Microsoft365.Views.Microsoft365View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:SysAdminX.Microsoft365.ViewModels"
    mc:Ignorable="d"
    Title="Microsoft 365"
    d:DataContext="{d:DesignInstance vm:Microsoft365ViewModel, IsDesignTimeCreatable=False}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        
        <Style x:Key="LicensedIconStyle" TargetType="ui:SymbolIcon">
            <Setter Property="Symbol" Value="DismissCircle24" />
            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLicensed}" Value="True">
                    <Setter Property="Symbol" Value="CheckmarkCircle24" />
                    <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Microsoft 365"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="Manage Exchange Online mailboxes and Azure AD users."
                           FontSize="13"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Margin="0,4,0,0" />
            </StackPanel>
        </Grid>
        
        <!-- Warning Banner -->
        <ui:InfoBar Grid.Row="1" Title="M365 Modules Missing or Not Connected"
                    Message="Please install ExchangeOnlineManagement or Microsoft.Graph modules and Connect-ExchangeOnline first."
                    Severity="Warning" IsOpen="{Binding IsMissingModules}" Margin="0,0,0,20" />

        <!-- Search Bar -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <ui:TextBox Grid.Column="0" 
                        PlaceholderText="Search by DisplayName or UPN..." 
                        Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                        Icon="Search24" 
                        ClearButtonEnabled="True" 
                        Margin="0,0,16,0" />
                        
            <ui:Button Grid.Column="1" Command="{Binding SearchCommand}" Appearance="Primary" Width="100">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Search24" Margin="0,0,8,0" />
                    <TextBlock Text="Search" />
                </StackPanel>
            </ui:Button>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="24" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            
            <ui:ProgressRing Grid.RowSpan="3" IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

            <!-- Users List -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    x:Name="UsersBorder">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                    
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <ui:SymbolIcon Symbol="Person24" Margin="0,0,8,0"/>
                        <TextBlock Text="Users &amp; Licenses" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Licensed" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ui:SymbolIcon Style="{StaticResource LicensedIconStyle}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="DisplayName" Binding="{Binding DisplayName}" Width="*" />
                            <DataGridTextColumn Header="UPN" Binding="{Binding UserPrincipalName}" Width="*" />
                            <DataGridTextColumn Header="Login Status" Binding="{Binding BlockCredential}" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Mailboxes List -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    x:Name="MailboxesBorder">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                    
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <ui:SymbolIcon Symbol="Mail24" Margin="0,0,8,0"/>
                        <TextBlock Text="Exchange Mailboxes" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Mailboxes}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*" />
                            <DataGridTextColumn Header="Email" Binding="{Binding PrimarySmtpAddress}" Width="*" />
                            <DataGridTextColumn Header="Item Count" Binding="{Binding ItemCount}" Width="100" />
                            <DataGridTextColumn Header="Total Size" Binding="{Binding TotalItemSize}" Width="150" />
                            <DataGridTextColumn Header="Warning Quota" Binding="{Binding IssueWarningQuota}" Width="150" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
