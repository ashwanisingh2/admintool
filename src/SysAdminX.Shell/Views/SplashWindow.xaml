<Window x:Class="SysAdminX.Shell.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="SysAdminX"
        Width="480"
        Height="280"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="Transparent"
        AllowsTransparency="True"
        Opacity="0">

    <Border Background="{DynamicResource ApplicationBackgroundBrush}"
            CornerRadius="12"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            Padding="32">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- App icon + name -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:SymbolIcon Symbol="Apps24" FontSize="36" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <TextBlock Text="SysAdminX" FontSize="28" FontWeight="SemiBold"
                           Margin="12,0,0,0" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </StackPanel>

            <!-- Tagline -->
            <TextBlock Grid.Row="1"
                       Text="IT Admin Toolkit"
                       FontSize="14"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center" />

            <!-- Spinner + loading text -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                <ui:ProgressRing IsIndeterminate="True" Width="20" Height="20" />
                <TextBlock x:Name="LoadingText"
                           Text="Initializing services..."
                           FontSize="12"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
